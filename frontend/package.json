{
  "name": "a-demo-app-for-vonage-n-abridge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@emotion/react": "^11.10.0",
    "@emotion/styled": "^11.10.0",
    "@mui/material": "^5.10.0",
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "@opentok/client": "^2.23.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "build:local": "rm -rf ./build && rm -rf ../public && PUBLIC_URL=http://localhost:3002 npm run build && cp -R ./build ../public",
    "build:heroku": "rm -rf ./build && rm -rf ../public && PUBLIC_URL=https://video-with-medical-notes.herokuapp.com npm run build && cp -R ./build ../public",
    "build:neru":   "rm -rf ./build && rm -rf ../public && PUBLIC_URL=https://neru-b617e2b2-video-with-medical-notes-dev.use1.serverless.vonage.com npm run build && cp -R ./build ../public"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
